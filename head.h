#define EOF (-1)
#define OK 1
#define ERROR 0
#define OVERFLOW -1
typedef int Status;

extern void printCount(char path[]);
extern void printWord(char path[]);
extern void printLine(char path[]);
extern void printMore(char path[]);
